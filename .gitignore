/.idea
node_modules/
dist/
.env
.env.*
!.env.example
*.log
coverage/
.DS_Store
# Belt and braces: a bridge-token accidentally redirected into the checkout (docs pipe it instead).
token*.txt

# Private working kitchen (specs, plans, SDD ledger) — never published.
.superpowers/
